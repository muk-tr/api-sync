info:
  name: get /v1/credit-policies/:id
  type: http
  seq: 1
  tags:
    - CreditPolicies

http:
  method: GET
  url: "{{baseUrl}}/v1/credit-policies/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
