info:
  name: Get an extension request
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: GET
  url: "{{baseUrl}}/v1/loans/:loanId/has-pending-request"
  params:
    - name: loanId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
