info:
  name: Get a transaction on this loan by ID
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: GET
  url: "{{baseUrl}}/v1/loans/:loanId/transactions/:transactionId"
  params:
    - name: loanId
      value: ""
      type: path
    - name: transactionId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
