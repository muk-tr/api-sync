info:
  name: Get the expected repayments for this loan
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: GET
  url: "{{baseUrl}}/v1/loans/:loanId/expected-repayments"
  params:
    - name: loanId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
