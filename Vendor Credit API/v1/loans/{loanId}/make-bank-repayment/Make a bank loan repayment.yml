info:
  name: Make a bank loan repayment
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: POST
  url: "{{baseUrl}}/v1/loans/:loanId/make-bank-repayment"
  params:
    - name: loanId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 0,
        "accountNumber": "",
        "transactionReference": "",
        "description": "",
        "repaidAt": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
