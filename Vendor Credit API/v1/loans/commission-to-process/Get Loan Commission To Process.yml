info:
  name: Get Loan Commission To Process
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: GET
  url: "{{baseUrl}}/v1/loans/commission-to-process"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
