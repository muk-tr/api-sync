info:
  name: Get All Platform Loans And Commission
  type: http
  seq: 1
  tags:
    - Loans

http:
  method: GET
  url: "{{baseUrl}}/v1/loans/platform-loans"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
