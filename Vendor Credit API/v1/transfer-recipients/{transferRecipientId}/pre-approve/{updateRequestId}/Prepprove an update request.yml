info:
  name: Prepprove an update request
  type: http
  seq: 1
  tags:
    - TransferRecipients

http:
  method: POST
  url: "{{baseUrl}}/v1/transfer-recipients/:transferRecipientId/pre-approve/:updateRequestId"
  params:
    - name: transferRecipientId
      value: ""
      type: path
    - name: updateRequestId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
