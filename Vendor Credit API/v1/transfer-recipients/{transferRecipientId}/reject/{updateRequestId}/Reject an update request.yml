info:
  name: Reject an update request
  type: http
  seq: 1
  tags:
    - TransferRecipients

http:
  method: POST
  url: "{{baseUrl}}/v1/transfer-recipients/:transferRecipientId/reject/:updateRequestId"
  params:
    - name: transferRecipientId
      value: ""
      type: path
    - name: updateRequestId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "reason": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
