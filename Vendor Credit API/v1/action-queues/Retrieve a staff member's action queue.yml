info:
  name: Retrieve a staff member's action queue
  type: http
  seq: 1
  tags:
    - ActionQueues

http:
  method: GET
  url: "{{baseUrl}}/v1/action-queues"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
