info:
  name: Get all active loan products
  type: http
  seq: 1
  tags:
    - LoanProducts

http:
  method: GET
  url: "{{baseUrl}}/v1/loan-products/active"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
