info:
  name: Get approval workflow by ID
  type: http
  seq: 1
  tags:
    - ApprovalWorkflows

http:
  method: GET
  url: "{{baseUrl}}/v1/approval-workflows/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
