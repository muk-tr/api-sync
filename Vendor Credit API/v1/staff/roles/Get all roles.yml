info:
  name: Get all roles
  type: http
  seq: 2
  tags:
    - Staff

http:
  method: GET
  url: "{{baseUrl}}/v1/staff/roles"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
