info:
  name: Delete an existing role
  type: http
  seq: 3
  tags:
    - Staff

http:
  method: DELETE
  url: "{{baseUrl}}/v1/staff/roles/:roleId"
  params:
    - name: roleId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
