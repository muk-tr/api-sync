info:
  name: Retrieve all staff groups
  type: http
  seq: 1
  tags:
    - Staff

http:
  method: GET
  url: "{{baseUrl}}/v1/staff/groups"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
