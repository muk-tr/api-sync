info:
  name: Get allowed IP addresses for an Integration Partner by ID
  type: http
  seq: 2
  tags:
    - IntegrationPartners

http:
  method: GET
  url: "{{baseUrl}}/v1/integration-partners/:partnerId/allowed-ip-addresses"
  params:
    - name: partnerId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
