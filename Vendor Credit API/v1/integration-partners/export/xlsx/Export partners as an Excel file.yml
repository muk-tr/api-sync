info:
  name: Export partners as an Excel file
  type: http
  seq: 1
  tags:
    - IntegrationPartners

http:
  method: GET
  url: "{{baseUrl}}/v1/integration-partners/export/xlsx"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
