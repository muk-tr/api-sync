info:
  name: Retrieve businesses entity count
  type: http
  seq: 1
  tags:
    - Businesses

http:
  method: GET
  url: "{{baseUrl}}/v1/businesses/entities/count"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
