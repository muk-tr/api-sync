info:
  name: Verify a business profile
  type: http
  seq: 1
  tags:
    - Businesses

http:
  method: POST
  url: "{{baseUrl}}/v1/businesses/:businessId/verify"
  params:
    - name: businessId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
