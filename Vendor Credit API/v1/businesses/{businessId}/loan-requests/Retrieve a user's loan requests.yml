info:
  name: Retrieve a user's loan requests
  type: http
  seq: 1
  tags:
    - Businesses

http:
  method: GET
  url: "{{baseUrl}}/v1/businesses/:businessId/loan-requests"
  params:
    - name: businessId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
