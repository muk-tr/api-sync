info:
  name: Retrieve a business profile's documents
  type: http
  seq: 1
  tags:
    - Businesses

http:
  method: GET
  url: "{{baseUrl}}/v1/businesses/:businessId/documents"
  params:
    - name: businessId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
