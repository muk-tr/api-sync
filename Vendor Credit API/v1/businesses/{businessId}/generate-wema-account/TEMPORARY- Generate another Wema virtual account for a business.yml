info:
  name: "TEMPORARY: Generate another Wema virtual account for a business"
  type: http
  seq: 1
  tags:
    - Businesses

http:
  method: POST
  url: "{{baseUrl}}/v1/businesses/:businessId/generate-wema-account"
  params:
    - name: businessId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
