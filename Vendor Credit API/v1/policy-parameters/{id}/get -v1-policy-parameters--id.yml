info:
  name: get /v1/policy-parameters/:id
  type: http
  seq: 1
  tags:
    - PolicyParameters

http:
  method: GET
  url: "{{baseUrl}}/v1/policy-parameters/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
