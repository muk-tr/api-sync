info:
  name: put /v1/policy-parameters/:id
  type: http
  seq: 2
  tags:
    - PolicyParameters

http:
  method: PUT
  url: "{{baseUrl}}/v1/policy-parameters/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "name": "",
        "isSystemDerived": false,
        "isActive": false,
        "createdBy": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
