info:
  name: get /v1/policy-parameters
  type: http
  seq: 2
  tags:
    - PolicyParameters

http:
  method: GET
  url: "{{baseUrl}}/v1/policy-parameters"
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
    - name: search
      value: ""
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
