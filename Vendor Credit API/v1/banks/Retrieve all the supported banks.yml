info:
  name: Retrieve all the supported banks
  type: http
  seq: 1
  tags:
    - Banks

http:
  method: GET
  url: "{{baseUrl}}/v1/banks"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
