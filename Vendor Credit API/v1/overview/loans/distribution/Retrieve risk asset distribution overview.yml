info:
  name: Retrieve risk asset distribution overview
  type: http
  seq: 1
  tags:
    - Overview

http:
  method: GET
  url: "{{baseUrl}}/v1/overview/loans/distribution"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
