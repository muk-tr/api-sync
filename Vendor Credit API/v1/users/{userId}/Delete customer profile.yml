info:
  name: Delete customer profile
  type: http
  seq: 3
  tags:
    - Users

http:
  method: DELETE
  url: "{{baseUrl}}/v1/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
