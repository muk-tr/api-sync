info:
  name: Get the active investment rate guide
  type: http
  seq: 1
  tags:
    - Investments

http:
  method: GET
  url: "{{baseUrl}}/v1/investments/rate-guide/active"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
