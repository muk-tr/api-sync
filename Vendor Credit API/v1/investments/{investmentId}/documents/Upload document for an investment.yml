info:
  name: Upload document for an investment
  type: http
  seq: 2
  tags:
    - Investments

http:
  method: POST
  url: "{{baseUrl}}/v1/investments/:investmentId/documents"
  params:
    - name: investmentId
      value: ""
      type: path
  body:
    type: multipart-form
    data:
      - name: Type
        type: text
        value: ""
      - name: File
        type: file
        value: []
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
