info:
  name: Get a summary overview of an appraisal
  type: http
  seq: 1
  tags:
    - LoanAppraisals

http:
  method: GET
  url: "{{baseUrl}}/v1/loan-appraisals/:externalId/summary"
  params:
    - name: externalId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
