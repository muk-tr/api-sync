info:
  name: Get a credit policy analysis result
  type: http
  seq: 1
  tags:
    - LoanAppraisals

http:
  method: GET
  url: "{{baseUrl}}/v1/loan-appraisals/:externalId/credit-policy-analysis"
  params:
    - name: externalId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
