info:
  name: Enable multi-factor authentication for a staff account
  type: http
  seq: 1
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/mfa/enable"
  body:
    type: json
    data: |-
      {
        "otpCode": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
