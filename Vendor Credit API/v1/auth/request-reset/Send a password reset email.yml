info:
  name: Send a password reset email
  type: http
  seq: 1
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/v1/auth/request-reset"
  body:
    type: json
    data: |-
      {
        "emailAddress": "danielmomodu@yopmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
