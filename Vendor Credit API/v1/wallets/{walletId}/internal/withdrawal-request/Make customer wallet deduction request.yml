info:
  name: Make customer wallet deduction request
  type: http
  seq: 1
  tags:
    - Wallets

http:
  method: POST
  url: "{{baseUrl}}/v1/wallets/:walletId/internal/withdrawal-request"
  params:
    - name: walletId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 0,
        "bankName": "",
        "accountNumber": "",
        "accountName": "",
        "narration": "",
        "paidAt": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
