info:
  name: Create a wallet for a business
  type: http
  seq: 1
  tags:
    - Wallets

http:
  method: POST
  url: "{{baseUrl}}/v1/wallets/generate/business/:provider"
  params:
    - name: provider
      value: Wema
      type: path
    - name: provider
      value: Flutterwave
      type: path
      disabled: true
    - name: provider
      value: Keystone
      type: path
      disabled: true
    - name: provider
      value: Access
      type: path
      disabled: true
  body:
    type: json
    data: |-
      {
        "businessId": 0
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
